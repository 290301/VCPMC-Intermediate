import { useEffect } from 'react';
import { useSelector } from 'react-redux';
import { RootState } from '../../../redux/store';

const Playlist = () => {
      return <h2>Playlist Store</h2>;
};

export default Playlist;
